/*
 * Queue data structure (FIFO - first in first out)
 *
 * public methods:
 *
 *   add(i)    adds item i to the queue
 *   remove()  removes an item from the queue and returns it
 *   length()  returns current list length
 */

class Queue {

    constructor() {
    }

    add(item) {
    }
}


module.exports = Queue;
